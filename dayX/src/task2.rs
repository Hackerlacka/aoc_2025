use utils::{InputType, get_input_path};

pub fn run_task() {
    let path = get_input_path(1, 2, InputType::Regular, None);
    //let diffs = run_task_with_file(path);
    //info!("Result: {diffs}");
}
